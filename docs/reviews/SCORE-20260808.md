# 최종점검 성적표 — 한국사 스티커북: 시간여행 다이어리

> 작성일: 2026-08-08 · 근거: 직접 점검(이 세션의 빌드·브라우저 실측) + ship-check 체크리스트 매핑 · 이전: 없음(첫 성적표)
> 오늘 추가된 설정 패널(성취기준·교사 가이드·관리자 해금·고지)을 **반영한 현재 상태** 기준.

## 총점: 75/100

| # | 항목 | 점수 | 감점 사유 (근거 항목) |
|---|---|---|---|
| 1 | 학습효과 | 8/10 | A5 오답 피드백이 힌트형이지만 오답 "유형별" 구분은 없음 WARNING(−1) · A8 선택형 문항은 소거법으로 통과 가능(첫 시도만 기록되나 진행은 됨) WARNING(−1). A2 성취기준은 원문 대조 검증 통과(설정 패널 내 표기) |
| 2 | 재미·게임성 | 7/10 | A8 WARNING(−1) · 스테이지 내 난이도 상승 커브 미설계 WARNING(−1) · 재도전해도 기록 갱신 경로 없음 WARNING(−1). 상호작용 5유형(관찰·배치·선잇기·퍼즐·순서배열)은 PASS |
| 3 | 기능 완성도 | 7/10 | B11 초기화 확인팝업 실측 안 함 NOT TESTED(−1) · B12 5스테이지 전체 완주→수료 흐름 실측 안 함 NOT TESTED(−1) · B13 브라우저 뒤로가기 실측 안 함 NOT TESTED(−1). 핵심 경로 B3·B9·B10·B14는 실측 PASS |
| 4 | 로직·코드 품질 | 10/10 | E1~E11 전부 PASS(any 0개, 콘솔 오류 0, 빌드 통과, 데이터 분리, 소문자 경로). E12 테스트 없음 NOT APPLICABLE |
| 5 | 디자인·UI 일관성 | 8/10 | C4 버튼 겹침·C5 텍스트 잘림을 스크린샷으로 실측 못 함(브라우저 패널 미표시) NOT TESTED(−2). 토큰 CSS 단일 관리·공통 컴포넌트 사용은 PASS |
| 6 | 접근성 | 7/10 | C3 터치 44/48px 실측 안 함(코드상 48px 보장이나 규칙상 실측 필요) NOT TESTED(−1) · C9 글자 대비 시각 실측 안 함 NOT TESTED(−1) · C11 모달 포커스 트랩 없음 WARNING(−1). 색상 단독 구분 없음(✕·문구 병용)·reduced-motion 대응 PASS |
| 7 | 성능·용량 | 6/10 | D3 BGM 총 12MB WARNING(−1) · D5 bg12=bg13 동일 이미지 중복 WARNING(−1) · D6 미사용 에셋(bg02·bg04·bg13, 미매핑 아이콘 다수)이 public에 포함 WARNING(−1) · D9 애니메이션 프레임 실측 안 함 NOT TESTED(−1) |
| 8 | 초기화면 로딩속도 | 9/10 | 실측: 첫 화면 전송량 ~0.53MB(JS 218KB+CSS 23KB+bg 152KB+아이콘 4장 135KB) → 5MB 이하 PASS · BGM 1.6MB는 상호작용 후 스트리밍 · 지연 로딩 PASS. JS 로드 전 흰 화면(로딩 표시 없음) WARNING(−1) |
| 9 | 데이터 안전 | 9/10 | F8 초기화 버튼 실제 클릭 실측 안 함 NOT TESTED(−1). 개인정보 수집 0·외부 전송 0·version 필드·손상 복구 실측 PASS·출처 기록(CREDITS.md) PASS |
| 10 | 배포 준비 | 4/10 | 배포 전이라 미측정 다수: G1 배포 경로 NT(−1) · G10 프로덕션 빌드 실기기 확인 NT(−1) · G11 배포 후 smoke test NT(−1) · G17 origin 분리 문서화 NT(−1) · G18 카톡 썸네일 NT(−1) · G7 OG 이미지 없음 WARNING(−1). G8 description·G9 favicon은 오늘 추가로 PASS |

※ 절반 초과 NOT TESTED인 항목 없음 → 측정 불가 처리 항목 없음.
※ **이 문서는 점수다. 배포 가능/보류 판정은 ship-check로 별도 수행할 것.** (현재 FAIL 0개, 배포 차단 요소는 발견 안 됨)

## 개선 우선순위 (최저 3개)

1. **배포 준비 4/10** — 전부 "아직 배포를 안 해서" 깎인 것. Vercel 배포 + 배포 URL smoke test + OG/카톡 썸네일 1장이면 4→9. → `ship-check` 후 배포
2. **성능·용량 6/10** — bg13 삭제(중복), 미사용 에셋 build 제외 목록, BGM 비트레이트 낮추기(12MB→6MB 가능). → `asset-audit`
3. **접근성 7/10** — 브라우저 패널 열고 스크린샷 실측(C3·C9), Modal 포커스 트랩 추가. → `tablet-check`

## 측정하지 못한 것

| 근거 항목 | 왜 | 누가 언제 |
|---|---|---|
| C4·C5·C9 (겹침·잘림·대비) | 브라우저 패널이 화면에 표시되지 않아 스크린샷 불가 | 사용자가 미리보기 패널 열면 즉시 |
| B12 (완주→수료) | 5스테이지 28문항 전체 완주 미수행 | 다음 세션 자동 완주 스크립트로 |
| D1 저속망 로딩 | 학교 와이파이 환경 없음. 크기 기반 추정(0.53MB≈1초 이내)만 수행 | 실제 학교 기기에서 |
| G10~G18 | 배포 전 | 배포 직후 |
